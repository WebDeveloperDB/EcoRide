Plan EcoRide (maj 27.03.2026)

Fait
- US1: Accueil (hero, recherche, FAQ, newsletter) OK
- US2: Navigation SPA OK
- US3: Recherche covoiturages OK
- US5: Detail covoiturage + avis lies au trajet OK
- US6: Participer (Participation entity, credits, places) OK
- US7: Inscription/connexion OK
- US8: Espace utilisateur (profil, preferences, vehicules + photos) OK
- US9: Creation trajet (regles chauffeur/les_deux + admin) OK
- US10: Historique + annulation (base) OK
- US11: Statut trajet + demarrer/arrivee + avis passagers apres arrivee OK
- US12: Dashboard employe (auth X-AUTH-TOKEN + role checks employe/admin) OK
- US13: Stats admin + suspension comptes + creation employe guidee OK
- Admin trajets: boutons modifier sur pages principales OK

Fait dans ce bloc
- US10 etendu:
	- GET /api/utilisateur/historique?status=all|coming|past
	- support admin: filtre userId
	- regles annulation par role:
		- participant: annulation jusqu'a 2h avant depart
		- conducteur: annulation jusqu'au depart
		- admin: pas de bouton annulation trajet/participation (politique actuelle)
- Correction navigation detail:
	- Accueil -> Detail utilise maintenant le meme chemin que la page covoiturages
	- scripts home charges avec cache busting pour eviter ancien JS en cache
- Photos:
	- suppression des images statiques de secours (driver/car) dans les vues principales
- Admin CRUD centralise (nouveau):
	- API /api/admin/users (GET/POST/PUT/DELETE)
	- API /api/admin/vehicules (GET/POST/PUT/DELETE)
	- API /api/admin/trajets (GET/PUT)
	- API /api/admin/avis (GET/PUT/DELETE)
	- API /api/admin/participations (GET)
- Front admin:
	- route /EcoRide/Front/admin-dashboard
	- page dashboard admin avec actions CRUD sur les domaines ci-dessus
- US11 finalise dans detail trajet:
	- bouton admin visible uniquement pour ROLE_ADMIN
	- participation seulement sur trajet planifie
	- participation deja existante visible dans l'UI (deja participant + lien historique)
	- avis trajet autorise uniquement pour passager participant apres statut termine
- US12 finalise:
	- endpoints moderation avis restreints a ROLE_EMPLOYEE/ROLE_ADMIN
	- dashboard employe passe sur header X-AUTH-TOKEN
- US13 finalise:
	- endpoint stats admin: GET /api/admin/stats (utilisateurs, employes, chauffeurs, passagers, trajets)
	- suspension/reactivation compte: POST /api/admin/users/{id}/suspend et /unsuspend
	- creation employee guidee: POST /api/admin/employees
	- login bloque pour comptes suspendus (login + token auth)
	- dashboard admin: section statistiques + actions suspendre/reactiver + formulaire employee guide

Reste a faire
- US4: Filtres backend complets (note/duree fiables)
- Contact page + mentions legales finales
- Tests fonctionnels complets frontend/backend

Ordre propose (suite)
1. Stabilisation bloc admin (tests manuels + corrections UX)
2. US4 filtres backend complets
3. Finitions et recette complete

Regle suivi
- Mettre a jour ce fichier Plan.txt a chaque etape terminee.

Rappels workflow
- Messages de commit: francais simple
- Push apres validation utilisateur
